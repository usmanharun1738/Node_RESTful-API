# Server Configuration
PORT=3000

# Database Connection
# For local development with Docker:
# docker run -d --name mongodb -p 27017:27017 mongo:5.0
MONGO_URI=mongodb://localhost:27017/blogging-api

# For MongoDB Atlas (production):
# MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/blogging-api?retryWrites=true&w=majority

# JWT Configuration
# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=replace_this_with_a_secure_secret_at_least_32_characters_long
